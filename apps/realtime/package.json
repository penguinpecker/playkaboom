{
  "name": "@playkaboom/realtime",
  "version": "0.1.0",
  "private": true,
  "description": "WebSocket relay that fans out Supabase Realtime events (game settles, LP actions) to connected playkaboom.gg clients. Primary live-feed push; the web client also subscribes to Supabase Realtime directly as a fallback and polls at 60s as a last-resort safety net.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/ws": "^8.5.13",
    "tsx": "^4.21.0",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
